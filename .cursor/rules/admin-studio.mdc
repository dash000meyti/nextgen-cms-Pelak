---
description: قوانین Studio CMS
globs: packages/studio/**,apps/pelak/app/admin/**,apps/pelak/components/admin/**
alwaysApply: false
---

# Admin Studio — Nextgen CMS

`packages/studio` = package منطق admin · UI در `apps/pelak/components/admin/`

مرجع: `docs/STUDIO.md` · skills: `hokmran-studio`, `hokmran-rbac`

## مرزها

- **Public:** `@nextgen-cms/site-data` — هرگز mutations
- **Studio:** `packages/studio/` + `components/admin/` — هرگز `components/home/`

## Mutation pattern

```
requirePermissionMutation("…") → validate → repository write → invalidate*() → return | redirect
```

خطای اعتبارسنجی: `{ ok: false, error, field? }` — UI با `useFormFeedback` به `[data-field]` اسکرول می‌کند.

## RBAC

- `settings.*` — تب‌های هاب `/admin/settings/*` (۶ تب، شامل `settings.database`)
- `content.*` / `members.*` / `media.*` — بخش هسته
- `modules.*` — CRUD ماژول (`/admin/content-group`, `/admin/videos`)
- `settings.modules` — نام نمایشی + فعال/غیرفعال ماژول
- موضوعات: `/admin/content/topics` — مجوز `settings.content`

## Settings hub

- ۶ تب: `packages/studio/src/admin/settings-tabs.ts` (شامل پایگاه داده — `settings.database`)
- تنظیمات بخش: `/admin/content/settings`, `/admin/members/settings`, تب مدیا، `/admin/content-group/settings`, `/admin/videos/settings`
- layout: بدون هیچ `settings.*` → `forbidden()`

## Schema-driven

- Field defs: `packages/contract/src/cms-schema/`
- Validation: `packages/studio/src/cms/validation/`
- Mutations: `packages/studio/src/cms/mutations/`

## Auth

- `apps/pelak/proxy.ts` — JWT cookie
- `getMemberSession()` در layout و mutations
